"""It is not clear exactly what representation should be used for the Fraction,
so as long as it outputs a fraction, you get credit."""

from P9 import Fraction

print(repr(Fraction(1,2)))
# should output something like "1/2"